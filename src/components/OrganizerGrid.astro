---
export interface Props {
  /** # of columns at medium breakpoint (1–6) */
  cols?: 1|2|3|4|5|6;
}
const { cols = 3 } = Astro.props;

const mdColsMap: Record<1|2|3|4|5|6,string> = {
  1: 'md:grid-cols-1',
  2: 'md:grid-cols-2',
  3: 'md:grid-cols-3',
  4: 'md:grid-cols-4',
  5: 'md:grid-cols-5',
  6: 'md:grid-cols-6',
};

const classes = `grid grid-cols-1 ${mdColsMap[cols]} gap-4`;
---
<div class={classes}>
  <slot />
</div>
